#!/bin/bash
if [$# -ne 3]; then
   echo "illegal number of parameters";
else
	fext = `$1`;
	fsize = `$2`;

	for f in *.$fext
	do
		name=`echo "$f" | sed -e "s/.JPG$//g"`;
		convert "$f" -resize $fsize "$name.jpg";
	done
	rm *.$fext;
fi

